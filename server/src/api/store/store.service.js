//const pool = require("../../config/database");
//module.exports = {
    
//};


/*
console.log(data)
        pool.query(
            `SELECT Product.name, Product.unitPrice, Product.type 
            FROM Product
            JOIN Store ON (Product.storeID=Store.storeID)
            WHERE Store.name=Walmart`,
            (error, results, fields) => {
            if (!error) {
                res.send(rows);
            }
            else {
                console.log(err);
            }
*/